function isOldEnoughToDrink(age) {
    // your code here
    if (age >= 21){
        return true
    }else{
        return false
    }
    }


// 1- se puede usar return em ambos caosos porque solo uno se ejecutara 
//2- la entrada es age un numero que representa la edad de una persona
/*3-  la salida es un boleano no se imprime nada porque no lo estan pidiendo solo
se retorna el resultado */ 
/* 4- para resolver este ejercicio se necesita un condicional que 
evalue si la edad es igual o mayor a 21 y retorne el resultado
*/
/*5- a.Aprendi que puedo usar return dos veces prque al final solo uno se ejecutara
     b. Apreendi que en condicionales cuando uso else no se escriben condiciones
     ejemplo }else(edadNumerica <= 21){
     la siguiente condicion debe ir en un else if */